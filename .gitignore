# Secrets (safety net — nothing here should need them, but never commit them)
.env
.env.local
*credentials*.json
service-account*.json

# OS / editor cruft
.DS_Store
*.swp
*.swo
.vscode/
.idea/

# Logs
*.log

# Local ticket drafts / scratchpad (not tracked)
tickets/
